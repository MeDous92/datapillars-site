---
const nav = [
  { label: "Services", href: "/services" },
  { label: "Case Examples", href: "/case-examples" },
  { label: "About", href: "/about" },
  { label: "Contact", href: "/contact" },
];
---

<header class="border-b border-zinc-200">
  <div class="mx-auto flex max-w-6xl items-center justify-between px-4 py-4 md:px-6">
    <a href="/" class="flex items-center gap-2 font-semibold tracking-tight">
      <span class="inline-flex h-8 w-8 items-center justify-center rounded-lg bg-zinc-900 text-white">DP</span>
      <span>DataPillars</span>
    </a>

    <nav class="hidden items-center gap-6 md:flex">
      {nav.map((item) => (
        <a class="text-sm text-zinc-700 hover:text-zinc-900" href={item.href}>{item.label}</a>
      ))}
      <a href="/contact" class="rounded-lg bg-zinc-900 px-3 py-2 text-sm text-white hover:bg-zinc-800">
        Book a Call
      </a>
    </nav>

    <a href="/contact" class="md:hidden rounded-lg bg-zinc-900 px-3 py-2 text-sm text-white hover:bg-zinc-800">
      Contact
    </a>
  </div>
</header>
