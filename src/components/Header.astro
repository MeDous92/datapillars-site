---
const nav = [
  { label: "Services", href: "/services" },
  { label: "Case Examples", href: "/case-examples" },
  { label: "About", href: "/about" },
  { label: "Contact", href: "/contact" },
];
---

<header class="sticky top-0 z-50 border-b border-zinc-200/70 bg-white/70 backdrop-blur">
  <div class="mx-auto flex max-w-6xl items-center justify-between px-4 py-4 md:px-6">
    <a href="/" class="flex items-center gap-2 font-semibold tracking-tight">
      <span class="inline-flex h-9 w-9 items-center justify-center rounded-xl bg-zinc-900 text-white shadow-sm">DP</span>
      <span>DataPillars</span>
    </a>

    <nav class="hidden items-center gap-7 md:flex">
      {nav.map((item) => (
        <a class="text-sm text-zinc-700 hover:text-zinc-900" href={item.href}>{item.label}</a>
      ))}
      <a href="/contact" class="rounded-xl bg-zinc-900 px-4 py-2 text-sm text-white shadow-sm hover:bg-zinc-800">
        Book a Call
      </a>
    </nav>

    <a href="/contact" class="md:hidden rounded-xl bg-zinc-900 px-4 py-2 text-sm text-white shadow-sm hover:bg-zinc-800">
      Contact
    </a>
  </div>
</header>
