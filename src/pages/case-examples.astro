---
import BaseLayout from "../layouts/BaseLayout.astro";
import CTA from "../components/CTA.astro";
---

<BaseLayout title="Case Examples — DataPillars" description="Representative reporting engagements (anonymized).">
  <section class="max-w-3xl">
    <h1 class="text-3xl font-semibold tracking-tight md:text-4xl">Case Examples</h1>
    <p class="mt-3 text-zinc-700">
      Representative engagements (details anonymized). Each example focuses on outcomes: consistency, automation, and decision-ready reporting.
    </p>
  </section>

  <section class="mt-10 grid gap-6 md:grid-cols-3">
    {[
      {
        title: "Finance vs Ops numbers didn’t match",
        bullets: ["Aligned KPI definitions", "Fixed logic inconsistencies", "Created consistent reporting layer", "Delivered leadership dashboard"],
      },
      {
        title: "Manual monthly reporting took days",
        bullets: ["Removed Excel dependency", "Automated refresh", "Standardized reporting structure", "Reduced reporting cycle time"],
      },
      {
        title: "HR reporting lacked consistency",
        bullets: ["Defined HR metrics clearly", "Built stable reporting tables", "Created HR dashboards", "Added documentation for handover"],
      },
    ].map((c) => (
      <div class="rounded-3xl border border-zinc-200 bg-white/70 p-6">
        <div class="font-semibold">{c.title}</div>
        <ul class="mt-3 space-y-2 text-sm text-zinc-700">
          {c.bullets.map((b) => (
            <li>• {b}</li>
          ))}
        </ul>
      </div>
    ))}
  </section>

  <CTA />
</BaseLayout>
